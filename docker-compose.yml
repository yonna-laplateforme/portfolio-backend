services:
  app:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: portfolio-backend

    ports:
      - "3001:3001"

    env_file:
      - .env

    restart: unless-stopped